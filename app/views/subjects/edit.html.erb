<h2><%= t('view.subjects.edit_subject') %></h2>
<%= form_with(model: @subject, local: true) do |form| %>

  <% if @subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= @subject.errors.count %>
      <%= t('errors.messages.count') %></h2>
      <ul>

      <% @subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="user_name">
    <%= t('view.users.name') %>
    <br>
    <%= @subject.user.profile.name %>
  </div>
  <br>
  <div class="subject_name">
    <%= form.label :name %>
    <br>
    <%= form.text_field :name %>
  </div>
  <div class="subject_status">
    <%= form.label :status %>
    <%= form.select :status, Subject.statuses.keys %>
  </div>
  <div class="subject_deadline">
    <%= form.label :deadline %>
    <%= form.date_select :deadline, include_blank: ""%>
  </div>
  <div class="education_id">
    <%= form.label t('view.educations.education') %>
    <%= form.select :education_id,  Education.all.collect { |p| [ p.name, p.id ] }%>
  </div>

  <%= form.submit t('helpers.submit.update') %>
<% end %>
  <%= link_to t('helpers.back'), :back %>